import React, {Component} from 'react'
import _ from 'lodash'

import css from './GeometricBackground.css'

export default class Geometry extends Component {
    constructor(props) {
        super(props)
        let x = 0
        let y = 0
        let tris = []
    }

    render() {
        return (
            <svg className={css.Canvas}>
              {
              }
            </svg>
        )
    }
}
